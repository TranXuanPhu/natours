document.addEventListener('DOMContentLoaded', function () {
  //const formatYmd = (date) => date.toISOString().slice(0, 10);
  //$('#startDates').val(new Date(formatYmd(new Date())));
  //console.log(new Date(formatYmd(new Date())));
});
